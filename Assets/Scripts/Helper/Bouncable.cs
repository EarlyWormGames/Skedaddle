using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// <summary>
/// class to have see if an object has a "Bouncable" script attached to it
/// Used as a filter for "GetComponent" calls
/// </summary>
public class Bouncable : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
